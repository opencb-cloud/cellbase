use DB_CONFIG;

#########################################################
#####	HOMO SAPIENS		#############################
#########################################################
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Homo sapiens',
    -group   => 'core',
    -dbname  => "$HOMO_SAPIENS_CORE" );
    
new Bio::EnsEMBL::Variation::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Homo sapiens',
    -group   => 'variation',
    -dbname  => "$HOMO_SAPIENS_VARIATION" );
    
new Bio::EnsEMBL::Funcgen::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Homo sapiens',
    -group   => 'funcgen',
    -dbname  => "$HOMO_SAPIENS_FUNCTIONAL" );


#########################################################
#####	PAN TROGLODYTES		#############################
#########################################################
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Pan troglodytes',
    -group   => 'core',
    -dbname  => "$PAN_TROGLODYTES_CORE" );
    

#########################################################
#####	MACAC MULATA		#############################
#########################################################
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Macaca mulatta',
    -group   => 'core',
    -dbname  => "$MACACA_MULATTA_CORE" );


#########################################################
#####	MUS MUSCULUS		#############################
#########################################################
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Mus musculus',
    -group   => 'core',
    -dbname  => "$MUS_MUSCULUS_CORE" );

new Bio::EnsEMBL::Variation::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Mus musculus',
    -group   => 'variation',
    -dbname  => "$MUS_MUSCULUS_VARIATION",
    -no_cache => 1 );
    
new Bio::EnsEMBL::Funcgen::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Mus musculus',
    -group   => 'funcgen',
    -dbname  => "$MUS_MUSCULUS_FUNCTIONAL" );


#########################################################
#####	RATTUS NORVEGICUS		#########################
######################################################### 
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Rattus norvegicus',
    -group   => 'core',
    -dbname  => "$RATTUS_NORVEGICUS_CORE" );

new Bio::EnsEMBL::Variation::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Rattus norvegicus',
    -group   => 'variation',
    -dbname  => "$RATTUS_NORVEGICUS_VARIATION",
    -no_cache => 1 );

new Bio::EnsEMBL::Funcgen::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Rattus norvegicus',
    -group   => 'funcgen',
    -dbname  => "$RATTUS_NORVEGICUS_FUNCTIONAL" );


#########################################################
#####	CANIS FAMILIARIS		#########################
#########################################################
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Canis familiaris',
    -group   => 'core',
    -dbname  => "$CANIS_FAMILIARIS_CORE" );

new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Canis familiaris',
    -group   => 'variation',
    -dbname  => "$CANIS_FAMILIARIS_VARIATION" );
    
    
#########################################################
#####	SUS SCROFA		#########################
######################################################### 
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Sus scrofa',
    -group   => 'core',
    -dbname  => "$SUS_SCROFA_CORE" );

new Bio::EnsEMBL::Variation::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Sus scrofa',
    -group   => 'variation',
    -dbname  => "$SUS_SCROFA_VARIATION",
    -no_cache => 1 );

#########################################################
#####	DANIO RERIO		#################################
#########################################################
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Danio rerio',
    -group   => 'core',
    -dbname  => "$DANIO_RERIO_CORE" );

new Bio::EnsEMBL::Variation::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Danio rerio',
    -group   => 'variation',
    -dbname  => "$DANIO_RERIO_VARIATION",
    -no_cache => 1 );
    
   
#########################################################
#####	DROSOPHILA MELANOGASTER		#####################
#########################################################
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Drosophila melanogaster',
    -group   => 'core',
    -dbname  => "$DROSOPHILA_MELANOGASTER_CORE" );

new Bio::EnsEMBL::Variation::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Drosophila melanogaster',
    -group   => 'variation',
    -dbname  => "$DROSOPHILA_MELANOGASTER_VARIATION",
    -no_cache => 1 );

new Bio::EnsEMBL::Funcgen::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Drosophila melanogaster',
    -group   => 'funcgen',
    -dbname  => "$DROSOPHILA_MELANOGASTER_FUNCTIONAL" );


#########################################################
#####	CAENORHABDITIS ELEGANS		#####################
#########################################################
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Caenorhabditis elegans',
    -group   => 'core',
    -dbname  => "$CAENORHABDITIS_ELEGANS_CORE" );


#########################################################
#####	SACCHAROMYCES CEREVISIAE		#################
#########################################################
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Saccharomyces cerevisiae',
    -group   => 'core',
    -dbname  => "$SACCHAROMYCES_CEREVISIAE_CORE" );

new Bio::EnsEMBL::Variation::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Saccharomyces cerevisiae',
    -group   => 'variation',
    -dbname  => "$SACCHAROMYCES_CEREVISIAE_VARIATION",
    -no_cache => 1 );
    
     
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Bos taurus',
    -group   => 'core',
    -dbname  => "$BOS_TAURUS_CORE" );

new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Aspergillus fumigatus',
    -group   => 'core',
    -dbname  => "$ASPERGILLUS_FUMIGATUS_CORE" );

new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Fusarium oxysporum',
    -group   => 'core',
    -dbname  => "$FUSARIUM_OXYSPORUM_CORE" );





new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Anopheles gambiae',
    -group   => 'core',
    -dbname  => "$ANOPHELES_GAMBIAE_CORE" );

new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Anopheles gambiae',
    -group   => 'variation',
    -dbname  => "$ANOPHELES_GAMBIAE_VARIATION" );


new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Plasmodium falciparum',
    -group   => 'core',
    -dbname  => "$PLASMODIUM_FALCIPARUM_CORE" );

new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Plasmodium falciparum',
    -group   => 'variation',
    -dbname  => "$PLASMODIUM_FALCIPARUM_VARIATION" );

new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Cavia porcellus',
    -group   => 'core',
    -dbname  => "$CAVIA_PORCELLUS_CORE" );
    
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Equus caballus',
    -group   => 'core',
    -dbname  => "$EQUUS_CABALLUS_CORE" );
    
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Pongo pygmaeus',
    -group   => 'core',
    -dbname  => "$PONGO_PYGMAEUS_CORE" );
    
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Ornithorhynchus anatinus',
    -group   => 'core',
    -dbname  => "$ORNITHORHYNCHUS_ANATINUS_CORE" );
    
new Bio::EnsEMBL::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Arabidopsis thaliana',
    -group   => 'core',
    -dbname  => "$ARABIDOPSIS_THALIANA_CORE" );
    
new Bio::EnsEMBL::Compara::DBSQL::DBAdaptor(
    -host    => "$ENSEMBL_HOST",
    -port    => "$ENSEMBL_PORT",
    -user    => "$ENSEMBL_USER",
    -pass    => "$ENSEMBL_PASS",
    -species => 'Multi',
    -group   => 'compara',
    -dbname  => "$COMPARA" );
    
